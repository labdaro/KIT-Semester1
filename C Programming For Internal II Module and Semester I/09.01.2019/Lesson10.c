#include<stdio.h>
main()
{
	char in_str[21];
	puts("Enter a string of max 20 character");
	/*function to accept and displaying*/
	fflush(stdin); /*clear the buffer*/h
	puts(in_str);/*display input string*/
}
